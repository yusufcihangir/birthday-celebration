{
  "buildCommand": "git clone https://github.com/flutter/flutter.git /tmp/flutter && export PATH=$PATH:/tmp/flutter/bin && flutter config --enable-web && flutter pub get && flutter build web --release --no-tree-shake-icons",
  "outputDirectory": "build/web"
}
